apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enemy.fullname" . }}-test-connection"
  labels:
    {{- include "enemy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enemy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
